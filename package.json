{
  "name": "server",
  "version": "1.0.0",
  "description": "Statpro server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "S. Wilson",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
